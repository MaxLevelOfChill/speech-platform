{
  "name": "speech-platform-server",
  "version": "1.0.0",
  "description": "Pronunciation analysis server (Allosaurus + FFmpeg)",
  "main": "server.js",
  "scripts": {
    "start": "set PYTHONUTF8=1 && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  }
}